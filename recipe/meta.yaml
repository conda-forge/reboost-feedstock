{% set name = "reboost" %}
{% set version = "0.11.0" %}
{% set python_min = "3.10" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.org/packages/source/{{ name[0] }}/{{ name }}/reboost-{{ version }}.tar.gz
  sha256: 27dc0e6deff220fd05289306fc0d3acdafc682cb4f0c3c3680370bf3cf65c18c

build:
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation
  number: 0
  entry_points:
    - reboost-optical = reboost.optmap.cli:optical_cli
    - reboost = reboost.cli:cli

requirements:
  host:
    - python {{ python_min }}
    - setuptools >=77
    - setuptools-scm >=7
    - pip
  run:
    - legend-lh5io >=0.2
    - legend-pygeom-hpges
    - python >={{ python_min }}
    - hdf5plugin
    - colorlog
    - numpy
    - scipy
    - numba >=0.60
    - legend-pydataobj >=2.0.1
    - legend-pygeom-optics >=0.15.0
    - legend-pygeom-tools >=0.0.26
    - hist
    - dbetto
    - particle
    - pandas
    - matplotlib-base
    - pygama
    - pyg4ometry

test:
  imports:
    - reboost
  commands:
    - pip check
    - reboost -h
    - reboost-optical -h
  requires:
    - python {{ python_min }}
    - pip

about:
  summary: LEGEND Monte-Carlo simulation post-processing
  home: https://github.com/legend-exp/reboost
  license: GPL-3.0-only
  license_file: LICENSE
  doc_url: https://reboost.readthedocs.io
  dev_url: https://github.com/legend-exp/reboost

extra:
  recipe-maintainers:
    - gipert
    - ManuelHu
